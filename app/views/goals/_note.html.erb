<li id="note_<%=note.id%>"><%= note.content %>
<% if @appropriation.editable_by?(current_user) %>
  <%= link_to "Remove", appropriation_note_path(@appropriation, note), :method => "delete", :confirm => "Are you sure?", :remote => true %>
<%end%>
</li>
